# 代码更新日志

## v1.2.0 — 2026-09-09
### 新增
- 界面整体改为深色主题，与 HELIX 系列工具风格一致
- 顶部栏：左侧红色竖线 + 白色标题「HELIX音频清理工具」，右侧深红底标签「制作人微信：helix_music」
- 窗口标题同步改为「HELIX音频清理工具」

### 调整
- 主题引擎从 macOS aqua 改为 ttk clam，实现完全自定义深色配色
- 所有控件（按钮、输入框、复选框、列表、表格、日志区）统一深色化

## v1.1.0 — 2026-09-08
### 新增
- 新增选项「清除括号及括号内全部内容」，支持 `()`、`（）`、`[]`、`【】`、`{}`、`｛｝` 等括号类型；勾选后如 `Song (Remix)【tag】.mp3` → `Song.mp3`。默认不勾选，与原有「空壳/纯编号括号删除」独立。

## v1.0.2 — 2026-09-08
- 应用图标替换为用户提供的 `horse.icns`

## v1.0.1 — 2026-09-08
Bug 修复与规则调整。

### 修复
- **文件名被误加 `(1)` 后缀**：当文件本身无需改名时（如已清理过一次、再次运行），防重名逻辑误把文件自己判定为冲突，导致批量追加 `(1)`。修复为：清理结果与原名相同时直接保留，跳过防重名检测。

### 调整
- **前导编号规则**：不再把 `序号-BPM` 整体删除。新规则：
  - 序号后紧跟 **3 位数字**时视为 BPM（如 `03-138`、`04-140`、`05-128`），**保留 BPM**，仅删序号
  - 序号后为 **1~2 位数字**时视为曲目序号范围（如 `05-18`、`20-31`），**整体删除**
  - 纯序号（如 `01`、`10`、`19`、`32`）直接删除

### 测试
- 新增 BPM 保留 / 序号范围删除 / 已清理文件不加 `(1)` 的测试用例，全部通过

## v1.0.0 — 2026-09-08
初始版本发布。

### 新增
- **选择文件夹**：图形界面一键选择目标文件夹，自动扫描当前层全部音频文件（MP3/WAV/M4A/FLAC/OGG 等常见格式）
- **清除音频文件属性**：基于 mutagen 删除标签元数据；自动探测失败时按扩展名回退处理（ID3 / WAVE / FLAC / MP4 / OGG）
- **清理文件名**：
  - 自动检测并清除所有文件共同包含的类似项（如 `【taoqu98】`），检测结果默认选中、可取消
  - 手动输入自定义删除片段
  - 去除前导编号（`01`、`03-138` 等）
  - 删除空壳/纯编号括号（`[]`、`【】`、`(01)`）
  - 清理首尾多余符号、压缩连续空白
- **预览与勾选**：执行前展示“原文件名 → 新文件名”预览，点击行可取消个别文件
- **防重名保护**：改名冲突时自动追加 `(1)`、`(2)`
- **后台处理**：批量处理不阻塞界面，日志逐条显示结果
- 提供三种交付形态：双击即用的 `.app`、双击启动的 `.command` 脚本、源码 `main.py`

### 修复
- 自动探测公共片段后未默认选中，导致预览不生效 —— 改为检测到即默认选中
- mutagen 新版本异常名变更（`NoID3Error` → `ID3NoHeaderError`）导致无标签文件误报失败 —— 兼容两种异常名
- 顶层 `ttk` 导入缺失 —— 统一在模块顶部导入

### 已知限制
- 只处理所选文件夹当前层，不递归子文件夹
- 清除元数据不可逆，处理前建议备份
- WAV 无标签时显示“本就没有标签”，不报错
